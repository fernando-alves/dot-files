alias d='docker'
alias c='docker-compose'

function dsall {
  docker ps | awk '{ print $1 }' | tail -n +2 | xargs docker stop
}

function dclean {
  docker images -f "dangling=true" | tail -n +2 | awk '{ print $3 }' | xargs docker rmi -f
}

export -f dsall
export -f dclean
