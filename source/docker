alias d='docker'
alias c='docker-compose'

function dsall {
  docker container stop $(docker container ls --format "{{.ID}}" -q)
}

function dclean {
  docker image prune -f
  docker container prune -f
}

export -f dsall
export -f dclean
